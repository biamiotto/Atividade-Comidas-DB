generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Comidas {
  id           Int @id @default (autoincrement())
  nome         String
  tipo         String
  preco        Float
  descricao    String

@@map("comidas")
}